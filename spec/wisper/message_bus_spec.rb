require 'spec_helper'

describe Wisper::MessageBus do
  it 'has a version number' do
    expect(Wisper::MessageBus::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
